
  # testest

  This is a code bundle for testest. The original project is available at https://www.figma.com/design/daiL29vQTAzUzG29RvJ9rO/testest.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  